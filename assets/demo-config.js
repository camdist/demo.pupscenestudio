window.PUPSCENE_DEMO_CONFIG = {
  // Change this only if your Sales Funnel checkout is hosted somewhere else.
  checkoutUrl: 'https://pupscenestudio.site/payment.html',
  // Same-origin endpoint included as a Cloudflare/D1 scaffold in this package.
  eventEndpoint: '/api/demo-event',
  demoLimit: 10
};
